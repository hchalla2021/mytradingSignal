# MyTradingSignal Backend Requirements
# =====================================
# Python 3.10+
# Pin all versions for reproducible production builds

# Core Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.1

# Data Analysis
pandas==2.2.3
numpy==2.2.1
scipy==1.15.1

# HTTP Clients
httpx==0.28.1
requests==2.32.3

# WebSockets
websockets==14.2

# Auth
python-jose[cryptography]==3.3.0

# Utilities
python-dotenv==1.0.1
pytz==2024.2
watchdog==6.0.0

# Trading API
kiteconnect==5.0.1

# Rate Limiting
slowapi==0.1.9

# ── Optional ML stack for Strike Intelligence AI engine ──────────────────
# All three are OPTIONAL — strike_intelligence_ai.py degrades gracefully
# (NumPy-only fallback) when any are missing. Install on production hosts
# that have at least ~2GB free RAM headroom for the warm-start models.
#
# LightGBM is small (~5 MB wheel) and CPU-only — safe everywhere.
# PyTorch CPU is ~200 MB — needed for the LSTM next-move predictor.
# TensorFlow is ~500 MB and OPTIONAL (softmax path falls back to NumPy).
#
# To install on a deploy host:
#   pip install lightgbm==4.5.0 torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
# Add TensorFlow only if you really want the TF softmax path:
#   pip install tensorflow==2.17.0
lightgbm==4.5.0
# torch==2.4.1   # uncomment + install from CPU wheel index
# tensorflow==2.17.0  # optional, large dep

